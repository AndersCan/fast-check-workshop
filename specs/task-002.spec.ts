import * as fc from 'fast-check';

describe('JSON parse', () => {
  test('parse and unparse gives same result', () => {});
});

describe('parseDurationToText', () => {
  test('parseDurationToText works', () => {});
});
